import styled from 'styled-components';

const Container = styled.div``;
const Title = styled.h1`
    margin: 0;
    font-size: 24px;
    line-height: 36px;
    color: #282538;
    font-weight: normal;
    margin-top: 32px;
    margin-bottom: 32px;

    span {
        font-weight: bold;
    }
`;

const Frame = styled.div``;

export { Container, Title, Frame } 